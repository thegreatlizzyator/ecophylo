python3 setup.py sdist bdist_wheel
python3 -m twine upload --repository testpypi dist/*
python3 -m pip install --index-url https://test.pypi.org/simple/ --no-deps example-pkg-Gowa

# to update :
rm dist/*
python3 setup.py sdist bdist_wheel
python3 -m twine upload --repository testpypi dist/*

# wait some time for server update
python3 -m pip install --index-url https://test.pypi.org/simple/ --no-deps example-pkg-Gowa
# or 
pip install path_to/the_great.tar.gz
# check the version in the logs

# install from github, meme si pas encore sur de comment ca marche
pip install git+https://github.com/jkbr/httpie.git#egg=httpie
# git repo must be public

# for upload to an official repo
twine upload dist/*
# for install from Pypi
pip install [your-package]

# TODO @p:1 Planning
# plan de vignette et article avant le 25
# donc article avec resultat = extraits de vignette
#
# vignette : 2 parties : changement de tailles; migration/vicariance
# partie 1 : utiliser les SFS (R) et comparer a log serie (attendus de la theorie neutre d'hubbel)
# partie 2 : gros check de code sur la partie 2
#
# donc vignette en R avec reticulate ou alors jupyter
#
# creer les docs -> google pour l'article 
#                -> markdown sur git (choisir entre jupyter ou rmarkdown with reticulate)
# voir pour set.seed de msprime
#
# TODO @p:2
# test sur le format plutot que contenu
# dosimulate
# 1 colomn pour muta, 1 colone par periode 
#
# TODO @p:10
# ajouter save tree to dosimulate voir dans dev/
# merge branches
